# 怪物猎人：荒野 - 差分管理器 (Armor Variant Manager)

这是一个基于 REFramework 开发的《怪物猎人：荒野》(Monster Hunter Wilds) Lua 脚本，它的核心目的是将多个差分合进一个 MOD 里的工具，可以在游戏内一键切换对应的外观差分，并且只占一套装备的位置。

---

## 📖 使用方法（MOD 用户）

### 1. 安装本模组

- 下载本模组压缩包。
- 将压缩包内的 `reframework` 文件夹直接解压到游戏根目录（即 `MonsterHunterWilds.exe` 所在的目录）。
- 最终路径结构应为：`游戏目录/reframework/autorun/ArmorVariantManager.lua`。

### 2. 使用

- 在游戏中按Home键打开差分管理器面板。
- 左侧面板选择分组，右侧面板选择想要的预设即可。
<!-- - 在游戏中按 **Insert** 键打开 REFramework 菜单。
- 找到 **Script Generated UI** 栏目，点击展开 **装备差分管理器**。
- 点击**预设管理**，右侧为分组列表，左侧为该分组的预设，在右侧选择你想要调整的分组，在左侧选择你想要的预设，点击**设为默认**即可将选中的预设永久保存。 -->

### 3. F&Q

- **预设丢失**：使用盒子等工具更改了套装会导致当前预设失效，在当前套装无预设时会出现自动查找预设按钮，点击即可重新匹配预设。
- **自己保存的预设被覆盖掉了**：原因：盒子等 MOD 管理工具在启用/禁用 MOD 时会把所有 MOD 都重新装一遍。现在差分管理器会创建备份文件，如果检测到备份文件冲突，会提示还原备份，点击即可还原你自己的预设。
- **掉帧/卡顿**：经测试默认预设对帧数的影响不到 2fps，如需要调整在性能设置里将选项往右拉。

---

## 🛠️ 使用方法（MOD 作者）

### 1. 当前防具/武器部件

可以查看当前装备的材质列表，左侧的勾选框可操控当前装备的材质对应网格的显示/隐藏。

### 2. 预设

在材质列表中勾选好想要显示的效果，在创建新预设处输入名称，点击保存为新预设即可创建一个预设。

### 3. 默认分组

所有的材质在最开始时都会进入默认分组，默认分组不可删除。

- 适用于整套装备只有少量材质变化，或者所有材质都是统一切换的场景：比如整套防具只有一个简单的"显示/隐藏披风"需求。

### 4. 分组

点击新增分组，此时会进入材质选择状态，在防具列表里勾选想进行分组的材质，输入名称点击确认即可创建一个新的分组。

- 分组**会**将选中的材质从默认分组里抽离出来，这部分材质控制权交给对应的分组。
- 在默认分组中，能够看到材质被哪个分组占用。
- 适用于同一套装备有多个独立部位需要分别管理，且它们之间互不影响：比如腰部装饰有"长裙/短裙/无"3种，腿部有"长靴/短靴"2种 → 分别建组，互相独立切换。

### 5. 全局分组

点击新增分组时，在下方勾选全局分组，即可进入新增全局分组模式，操作与分组一样。

- 全局分组**不会**将选中的材质从默认分组里抽离出来，其他分组仍然能选择和控制全局分组里的材质。
- 全局分组具有最高的**隐藏优先级**，即被全局分组隐藏的，在任何状态下始终保持隐藏。
- 被全局分组隐藏的材质在其他分组下仍可被勾选，但仅记录状态，不会进行显示/隐藏替换。
- 适用于跨越多个分组的统一控制：比如分组里的例子上加上红黄蓝三种配色，那使用普通分组则需要分 3×3+2×3 种预设，这时则可以将换色这个功能放进全局分组内。

### 6. 变身管理

基础预设管理基础上的功能，可以根据游戏内的不同条件动态操控预设变化。

- 变身管理具有回退功能，以血量触发为例，当血量低于 80 时从 A 变为 B，那当血量高于 80 时自动回退到 A。
- 多状态并行时，优先级数字越小则表示优先级越高。

### 7. 预设文件

- 保存的预设会在 `reframework\data\ArmorVariantManager` 文件夹内。
- 装备以当前身体（Body）部位为准，以当前身体部位 ID 命名，比如 `ch03_060_0002.json`。
- 武器则直接记录当前武器 ID，比如 `it0300_0020`。

---

## 📌 更新日志
* **V4.1.1**：修复新的全局分组无法生效的问题。
* **V4.1.0**：修复新UI的一些问题。为说明文档新增了英文翻译。
* **V4.0.0**：
  1. 新增独立UI面板，默认使用Home键打开。
  2. 新增支持自定义名字的装备。
  3. 新增自动保存预设功能，默认启用。 
  4. 修复亿点 bug。
* **V3.3.0**：新增备份与恢复功能，修复全局分组在变身管理中使用的BUG。
* **V3.2.0**：
  1. 新增全局分组功能，创建分组时可以将当前分组勾选为全局分组。
  2. 优化 UI 界面的操作性：材质列表支持全选/反选，按输入筛选，可以覆盖保存当前预设，分组和预设支持排序。
  3. 修复一些 bug。
* **V3.1.0**：更新支持武器差分，修复一些 bug。
* **V3.0.0**：引入全新的"变身管理"系统，支持基于条件（生命值、受击、武器拔刀/收刀，太刀气刃等级，大剑蓄力状态等武器）的动态外观切换。
* **V2.1.1**：修复了多人模式下的一些 bug。
* **V2.0.0**：
  1. 新增分组功能，可以将选中的选项抽离出一个分组，可以为新的分组单独配置预设。
  2. 去掉了加载按钮，现在选中即加载。
  3. 开放性能配置选项，可按照说明结合实际 CPU 性能调整，该选项直接关联着切换装备后加载预设的速度。
* **V1.2.1**：修复潜在的 bug，优化性能。
* **V1.2.0**：增加自动查找预设按钮，可以从 json 列表里查找匹配的预设，用于盒子等工具修改套装后恢复预设功能。
* **V1.1.0**：修复主菜单/存档界面不生效的问题。
* **V1.0.0**：发布。

---

## 🙏 致谢

* [MDF-XL](https://github.com/SilverEzredes/MDF-XL) - 本项目参考了其部分代码逻辑，特此感谢。
* [reframework-d2d](https://github.com/cursey/reframework-d2d) - 本项目使用了其代码用于设计UI面板。

## 🔗 链接

* GitHub：https://github.com/Monkeysama/ArmorVariantManager
